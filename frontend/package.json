{
  "name": "kateikyoshi-kun",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "esbuild src --outdir=dist --bundle --minify",
    "test": "vitest",
    "lint": "eslint 'src/**/*.{js,ts}'",
    "watch": "chokidar 'src/**/*' -c 'vite build && touch dist/manifest.json'"

  },
  "keywords": [
    "chrome-extension",
    "typescript",
    "vitest"
  ],
  "author": "motsupot",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.195",
    "chokidar-cli": "^3.0.0",
    "esbuild": "^0.18.11",
    "eslint": "^9.18.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^0.33.0"
  }
}
